from shared.config import settings

class APISettings:
    # Adding any API specific settings here if needed
    API_TITLE: str = "Bug Bounty Recon Platform API"
    API_VERSION: str = "1.0.0"

api_settings = APISettings()
